# 普通入口真实 RSI 闭环：首次探索性机制样本

日期：2026-09-24。使用本机 `mimo-v2.6-flash`，从普通 `nexgent task` 入口运行求和与均值任务；项目一次性配置包通道、改进器和独立 Workbench 评价器，没有手工提供反馈、候选或评价 ID。凭据只从本机 Manyselves 配置注入进程，未写入仓库。原始 SQLite Episode 与演化收据位于本机 `E:/PKU/program/2026/Aug/te/NExgent-live-rsi-20260924/.nexgent`；仓库内保存[脱敏摘要](receipt-20260924.json)。

普通 Episode 完成，3 次模型调用，结果为 `sum=16`、`mean=5.33`。终态触发器捕获公开反馈；独立的 R0 开发 Episode 使用 1 次模型调用，选择从本次任务的工作流形成编排候选。候选已生成，系统自动建立 Workbench selection 计划并运行父、候选两臂。两臂均未被独立评估接受，得分均为 0；候选没有完成有效行为激活。因此决策拒绝晋升，通道仍为 revision 0，guard 与后续复用没有发生。这是正确的拒绝路径，不是 RSI 收益。

这次源任务是算术，selection 却是 Workbench 发票核对；其用途只是证明普通入口确实走到模型规划、候选生成和独立拒绝。任务族不匹配，也没有基线对照或重复样本，不能据此评价编排方案的迁移效果。下一步应将来源任务与评价任务族对齐，并分别检验成功候选的 guard／下一任务复用，以及固定编排、单智能体和仅记忆对照。框架核心没有引用 Workbench；评价器由宿主配置注入。
