# Nexgent 整体重构：协作计划与验收依据

状态：进行中。上一轮有限策略原型未达到用户要求，不作为交付版本。

## 目标与纠偏

唯一产品是面向科研发现的 RSI 智能体系统。用户提出研究目标、资源预算和外部约束，系统承担资料研究、问题分解、假说、实验、反驳、复现、知识更新和智能体自身的改进。默认交互是研究信息空间。

上一轮的具体错误：

1. 把整体重构缩减为在旧包上增加 `rsi/` 和一个窗口，旧 Harness 依赖仍然支配仓库。
2. 把可改对象缩成五个数值参数和三项搜索配置，真正改进器与角色编排没有进入演化对象。
3. 把固定算法的代理测量称为元能力，没有运行被测版本的真实改进程序生成后代。
4. 没有把论文机制落实成模块责任、算法选择和必要消融；文献与实现脱节。
5. 用软件检查与流程完成替代研究验收；在真实任务未改进时结束了原请求。

保留旧结果作为失败证据和对照，不继续以旧协议限制新系统。

## 分工、依赖与交付物

| 工作线 | 负责人 | 当前产物 | 下游使用者 |
|---|---|---|---|
| 外部 RSI 研究 | RSI 子智能体 | `docs/research/reboot-rsi-mechanisms.md`：原文机制、软件自引用、真实元评价、对照与创新假设 | 根代理与改进器实现 |
| 科研协议与失败分析 | 科学研究子智能体 | `docs/research/reboot-scientific-protocol.md`：旧运行逐候选诊断、科学任务、独立评价、消融 | 科研执行器与实验负责人 |
| 全仓架构分析 | 架构子智能体 | `docs/research/reboot-repository-map.md`：完整迁移表、依赖、旧产品退场、产品接口 | 根代理、运行内核与界面 |
| 统一设计与整合 | 根代理 | 本计划、共享接口、源码包执行与预算内核、系统整合、研究验收 | 所有实现工作线 |

先汇总研究与审计，再冻结共享接口。每份代码指定一个写入负责人；跨模块修改先通过接口消息协调。实现后交换审查责任，避免模块作者自行宣布科学成功。

## 阶段与完成条件

### A. 研究与问题诊断

- 从一手全文核对 STOP、DGM、Hyperagents、HGM、AI Scientist 等工作的实际算法与评价。
- 将“论文机制 → 本系统对象 → 执行接口 → 观测证据 → 消融”逐项对应。
- 分析旧真实运行为什么停在原任务策略，区分提议能力不足、错误元指标和晋升规则造成的停滞。
- 给出创新假设、已存在的先例和能够否定假设的结果。创新不能由命名或文档数量证明。

出口：三份研究/审计文档及统一设计决策；不以补写综述充当实现。

### B. 整体架构与源码可执行契约

- 迁移为标准 `src/nexgent` 单一产品；旧包、旧入口、旧业务工具和旧产品测试从活动仓库退出，保留仓库外快照。
- 版本对象是内容寻址的**智能体源码包**，包含科研求解程序、改进程序和编排逻辑/提示。后代能够改写这些程序的算法与控制流。
- 当前版本的 `improve` 必须实际读取自己的源码与证据、调用科研/模型能力并生成下一版；下一轮从后代源码加载 `improve`。
- 角色分解、并行与依赖、证据传递、实验选择、父代选择属于可演化程序。宿主内核仅提供执行、预算、持久化、能力边界、独立评分和不可篡改证据。
- 科研任务只收到观测与公开问题；隐藏标签与最终评价由独立评价进程持有。
- 候选源代码可以定义函数、算法、循环与组合方式，不再从预设 TaskPolicy 枚举中选值。

出口：接口合同、最小纵向执行验证、旧目录的迁移清单。

### C. 并行重构实现

1. 内核：源码包、内容地址、独立进程、能力代理、预算与停止、持久事件/产物、恢复。
2. 科研系统：观察、实验工具、可执行科学提交、独立评分、文献证据与研究报告。
3. RSI 系统：可修改的改进器、动态多智能体编排、真实后代档案与父代选择、改进归因。
4. 产品：目标与资源入口、研究问题/假说/实验/结论/知识/版本谱系的信息窗口；模型与实验细节由系统管理。

出口：可实际执行的科学任务与源码自修改闭环。模型回复、被写入源码、被执行源码、评分对象、晋升对象必须一一可追踪。

### D. 科研实验与迭代

固定评价协议与总成本预算，开展：

- 强静态科学求解器基线；
- 固定改进器，只允许任务程序演化；
- 任务与改进器均可演化；
- 对提出的研究机制做去除/替换消融。

记录多随机种子、未见任务、数值误差、有效提交率、总计算/模型成本、真实后代提升概率与谱系。元能力必须运行被测改进器，在相同起点和预算下真实生成后代，再做开发选择与独立迁移评价。

不能再次用弱初始化对照掩盖强基线，不用同一个最终测试集反复调规则。失败触发有证据的诊断与新假说，再注册下一实验；保留历史失败，不把流程完成标为科研成功。

出口：源码继承和真实自修改成立；针对原请求提供实际研究改进证据，并准确报告对照、归因和没有得到支持的假设。测试通过不能替代此出口。

### E. 整体交付检查

- 活动仓库、安装包、入口和文档只描述同一科研 RSI 产品。
- 运行默认窗口，完成一个真实 Provider 科研/自改进任务，核对所有可见结果与证据。
- 检查源码包隔离、冻结评分、预算耗尽、停止恢复、错误提议与回归。
- 完成源码、研究材料、复现命令和当前状态的统一交付；不把未完成部分改写为范围限制后宣布完成。

## 正在形成的研究方向

以下是待验证的设计假设，不是已获证实的新理论：

- **模块替换实验用于改进归因**：分别组合父/子的科学程序与改进程序，运行真实后代生成，判断提升来自科学算法、研究组织还是二者共同作用。
- **根据实际后代证据选择研究父代**：将当前任务冠军与可用于继续探索的源码档案分开，避免用单一任务/代理分数阻止潜在有用分支；以真实后代结果更新信用。
- **以可反驳的科学结果指导自修改**：改进器读取实验失败、复杂度和成本证据，自行改变角色协作、实验设计及科研实现，而非只改一个“反思”标签。

文献已有机制会明确归属；本项目的贡献需要由实现差异与对照结果支撑。

## 执行状态

- [x] 承认旧交付未达标，恢复原始任务范围。
- [x] 启动三条独立研究/审计工作线。
- [x] 完成研究综合与共享源码契约。
- [x] 完成全仓迁移与并行实现。
- [ ] 完成源码级 RSI 和真实科研对照实验。
- [ ] 完成信息窗口及整体交付验收。
